[project]
requires-python = "==3.12.*"

dependencies = [
    "pandas",
    "psutil>=6.0.0",
]

[tool.pdm]
distribution = false

[tool.pdm.dev-dependencies]
dev = [
    "ipython>=8.26.0",
]
[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"

